pragma ComponentBehavior: Bound

import QtQuick
import qs.data
import qs.common
import qs.config

Text {
    text: "|"
    color: Colors.text
    font.family: Fonts.normalFont
    font.pointSize: 12
}
